// cars.length //returns the number of elements
// cars.sort() //sorts the arrays

const fruits = ["Banana", "Orange", "Apple", "Mango"];
let len = fruits.length
console.log(len);

//accessing the first and last elements in arrays
let first_fruit = fruits[0];
let last_fruit = fruits[len - 1]
console.log(first_fruit)
console.log(last_fruit)

//adding the elements using the push
fruits.push("Lemon");